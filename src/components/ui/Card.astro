---
interface Props {
  className?: string;
  hover?: boolean;
}

const { className = '', hover = true } = Astro.props;
---

<div class={`rounded-xl bg-white p-6 border border-border card-shadow ${hover ? 'cursor-pointer' : ''} ${className}`}>
  <slot />
</div>
